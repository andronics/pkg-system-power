name="power"
description="Power Management - Battery, brightness, and power control"
version="0.1.0"
method="stow"
collection="system"
fold=false

# Ignore documentation files
supported_os=("linux")
depends=()
requires_commands=("bspc" "systemctl")
ignore=(
    "README.md"
)
